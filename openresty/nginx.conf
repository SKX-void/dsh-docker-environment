# 这也是ai跑的，我没细看
# 主配置文件 /etc/nginx/nginx.conf
user nobody;
worker_processes auto;
error_log /dev/stderr warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
    use epoll;
    multi_accept on;
}

http {
    # 缓存DNS，而不是在启动时硬解析
    resolver 127.0.0.11 valid=10s;
    server_tokens off;

    # 基础设置， openresty 和 nginx 配置文件位置不一样
    include /usr/local/openresty/nginx/conf/mime.types;
    default_type application/octet-stream;

    # 日志格式
    log_format main '[$time_local] $status [$http_referer $request] '
    '$body_bytes_sent';

    access_log /dev/stdout main;

    # 性能优化
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    client_max_body_size 100M;

    # Gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/json application/xml+rss;

    # # docker拿不到ip
    # allow 192.168.0.0/16;
    # deny all;

    include sites/*.conf;


    server {
        listen 80 default_server;
        server_name _;
        set $lua_log "";
        log_by_lua_file /usr/local/openresty/lualib/custom/log.lua;
        return 404;
    }

    error_page 404 @error_404;

    location @error_404 {

        default_type text/html;

        return 404 '
        <!DOCTYPE html>
        <html>
        <head><meta charset="UTF-8"><title>dsh</title></head>
        <body>
        <h1>404</h1>
        <p>Not Found</p>
        </body>
        </html>
        ';
    }
}


# listen 3081 ssl;
# http2 on;
#     # SSL 证书配置（请替换为您的证书路径）
# ssl_certificate /usr/local/openresty/nginx/ssl/server.crt;      # 证书文件路径
# ssl_certificate_key /usr/local/openresty/nginx/ssl/server.key;  # 私钥文件路径
# # SSL 安全配置（推荐）
# ssl_protocols TLSv1.2 TLSv1.3;                  # 仅使用安全的 TLS 版本
# ssl_ciphers HIGH:!aNULL:!MD5:!RC4;              # 强加密套件
# ssl_prefer_server_ciphers on;
# ssl_session_cache shared:SSL:10m;
# ssl_session_timeout 10m;